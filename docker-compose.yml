services:
  backend:
    profiles: [backend]
    env_file:
      - ./backend/.env
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - '${BACKEND_HOST_PORT:-8000}:8000'
  frontend:
    profiles: [frontend]
    env_file:
      - ./frontend/.env
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '${FRONTEND_HOST_PORT:-80}:80'